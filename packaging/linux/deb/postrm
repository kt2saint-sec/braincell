#!/bin/sh
# postrm for braincell-mcp — remove the generated venv (it is created by
# postinst, so dpkg does not track its files). User data under
# ~/.local/share/braincell/ is NEVER touched.
set -e

case "$1" in
    remove|purge)
        rm -rf /opt/braincell-mcp
        ;;
esac

exit 0
